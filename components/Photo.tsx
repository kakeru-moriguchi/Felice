import {existsSync} from 'node:fs';import path from 'node:path';import {images} from '@/lib/site';
export function Photo({kind,label,className=''}:{kind:keyof typeof images;label:string;className?:string}){const isHero=kind==='hero';const src=images[kind];const exists=existsSync(path.join(process.cwd(),'public',src.replace(/^\//,'')));return <div className={`photo photo-${kind} ${className}`} style={exists?{backgroundImage:`url(${src})`}:undefined} role="img" aria-label={isHero?'落ち着いた雰囲気の施術室イメージ':exists?label:`${label}の写真掲載予定`}>{(isHero||!exists)&&<span className="photo-note">{isHero?'※ 仮イメージ写真':`${label} / PHOTO COMING SOON`}</span>}</div>}
