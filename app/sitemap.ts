import type {MetadataRoute} from 'next';import {getPosts} from '@/lib/blog';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||(process.env.VERCEL_URL?`https://${process.env.VERCEL_URL}`:'https://example.com');const paths=['','/therapist','/shop','/blog','/contact','/legal'];const posts=await getPosts();return [...paths.map(p=>({url:`${base}${p}`,lastModified:new Date()})),...posts.map(p=>({url:`${base}/blog/${p.slug}`,lastModified:new Date(p.publishedAt)}))]}
